name: Deploy Staging
on:
  push:
    branches: [ staging ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    needs: [e2e-test] # Depends on E2E passing (if we had it in same workflow or via workflow_run)
    steps:
      - name: Deploy to Staging
        run: echo "Deploying to staging server..."
        # Stub for actual deployment commands (e.g. SSH, AWS CLI, etc)
